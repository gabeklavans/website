<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ title }}</title>
        <link href="/style.css" rel="stylesheet">
    </head>
    <body>
        <nav>
            <a href="/">
            {% if page.url == "/" %}
                Welcome home
            {% else %}
                {{ backs | random }}
            {% endif %}
            </a>
        </nav>
        <hr>
        <main>
            {{ content | safe }}
        </main>
        <hr>
        <footer>
            {{ quotes | random | join(" - ") }}
        </footer>
    </body
</html>
<ul>
