<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ title }}</title>
        <link href="/style.css" rel="stylesheet">
    </head>
    <body>
        <nav>
            <a href="/">
            {% if page.url == "/" %}
                Welcome home
            {% else %}
                {{ [
                    "I wish to leave now",
                    "Please escort me back",
                    "Go back",
                    "Home",
                    "*leave*"
                    ] | random }}
            {% endif %}
            </a>
        </nav>
        {{ content | safe }}
    </body>
</html>
<ul>
